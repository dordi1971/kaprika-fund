export const metadata = {
  title: "PROPOSE // DEFINE",
};

export default function ProposeNewPage() {
  return (
    <main className="container">
      <div className="contentFrame">
        <section className="section">
          <span className="metaLabel">Creator</span>
          <h1 className="pageTitle">Define your project</h1>
          <p className="muted">
            Form building comes next. The current work establishes the tone, ordering, and
            friction rules.
          </p>
          <p className="muted">This screen intentionally does not exist yet.</p>
        </section>
      </div>
    </main>
  );
}

